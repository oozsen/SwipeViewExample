<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="SwipeViewExample.MainPage">

    <StackLayout Padding="10">
        <SwipeView>
            <SwipeView.LeftItems>
                <SwipeItems>
                    <SwipeItem Text="Left - 1" BackgroundColor="Red"/>
                    <SwipeItem Text="Left - 2" BackgroundColor="Yellow"/>
                </SwipeItems>
            </SwipeView.LeftItems>
            <SwipeView.RightItems>
                <SwipeItems>
                    <SwipeItem Text="Right" BackgroundColor="Black"/>
                </SwipeItems>
            </SwipeView.RightItems>
            <SwipeView.TopItems>
                <SwipeItems>
                    <SwipeItem Text="Top" BackgroundColor="Blue"/>
                </SwipeItems>
            </SwipeView.TopItems>
            <SwipeView.BottomItems>
                <SwipeItems>
                    <SwipeItem Text="Bottom" BackgroundColor="Green"/>
                </SwipeItems>
            </SwipeView.BottomItems>
            <BoxView BackgroundColor="Brown" WidthRequest="150" HeightRequest="150"/>
        </SwipeView>
    </StackLayout>

</ContentPage>
